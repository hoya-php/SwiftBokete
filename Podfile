# Uncomment the next line to define a global platform for your project
  platform :ios, '13.0'

target 'SwiftBokete' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftBokete
  
  pod 'Alamofire'
  pod 'SDWebImage'
  pod 'SwiftyJSON'

  plugin 'cocoapods-keys', {
   :project => "SwiftBokete",
   :keys => [
    "APIToken"
   ]}

  target 'SwiftBoketeTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
